<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Raw Intelligence" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Blog App Rails" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%# Modernizr is required for Zurb Foundation %>
    <%= javascript_include_tag 'vendor/modernizr' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    
    <main role="main">
      
      <div class="small-12 medium-8 large-9 columns" role="content">
       <%= render 'layouts/messages' %>
       <%= yield %>
      </div>

      <aside class="small-12 medium-4 large-3 columns">
        <%= render partial: "sidebar" %>
      </aside>

      <footer class="row">
        <%= render partial: "footer" %>
      </footer>
    
    </main>
  </body>
</html>
